# GitHub

Репозиторий: **https://github.com/Banifaziy111/brak**

```powershell
cd c:\Users\perepechkin.a2\Desktop\kurkuma
git push origin main
```

Файл `.env` с паролем БД **не** попал в git (только `.env.example`).

## Vercel

После push: https://vercel.com/new → Import `Banifaziy111/kurkuma` → добавить переменные `DB_*` из `.env.example`.

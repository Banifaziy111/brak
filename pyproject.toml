[project]
name = "brak"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "Flask==3.0.3",
    "Werkzeug==3.0.6",
    "psycopg2-binary==2.9.9",
    "python-dotenv==1.0.1",
    "openpyxl",
]

[tool.vercel.scripts]
install = "pip install -r requirements.txt"
